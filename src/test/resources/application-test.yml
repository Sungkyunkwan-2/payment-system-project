# application-test.yml
spring:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  application:
    name: payment-team-project

  profiles:
    active: test

  # AWS 완전 비활성화
  cloud:
    aws:
      credentials:
        access-key: test
        secret-key: test
      region:
        static: ap-northeast-2
        auto: false
      stack:
        auto: false
      parameterstore:
        enabled: false
      secretsmanager:
        enabled: false

  # config import 비활성화
  config:
    import: none

  # H2 인메모리 데이터베이스 사용
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver

  h2:
    console:
      enabled: true

  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    encoding: UTF-8

  security:
    user:
      name: demo
      password: demo123

  # AWS auto-configuration 제외
  autoconfigure:
    exclude:
      - org.springframework.cloud.aws.autoconfigure.context.ContextStackAutoConfiguration
      - org.springframework.cloud.aws.autoconfigure.context.ContextRegionProviderAutoConfiguration
      - org.springframework.cloud.aws.autoconfigure.context.ContextResourceLoaderAutoConfiguration
      - org.springframework.cloud.aws.autoconfigure.mail.MailSenderAutoConfiguration
      - org.springframework.cloud.aws.autoconfigure.paramstore.AwsParamStoreBootstrapConfiguration
      - org.springframework.cloud.aws.autoconfigure.secretsmanager.AwsSecretsManagerBootstrapConfiguration

server:
  port: 0  # 랜덤 포트 사용으로 테스트 충돌 방지

# PortOne Configuration - 테스트용 Mock 값
portone:
  api:
    base-url: https://api.portone.io
    secret: test-portone-secret-key-for-testing-only
  store:
    id: test-store-id
  channel:
    kg-inicis: test-kg-inicis-channel
    toss: test-toss-channel
  webhook:
    secret: test-webhook-secret-key

# JWT Configuration - 테스트용 값
jwt:
  # 최소 256비트(32바이트) Base64 인코딩된 시크릿
  secret: dGVzdC1qd3Qtc2VjcmV0LWtleS1mb3ItdGVzdGluZy1vbmx5LW11c3QtYmUtbG9uZy1lbm91Z2g=
  access-token-validity: 1800
  refresh-token-validity: 604800

# UI Branding Configuration
app:
  ui:
    branding:
      app-name: CommerceHub
      tagline: 통합 커머스 플랫폼
      logo-text: 🏪 CommerceHub

logging:
  level:
    com.bootcamp.paymentdemo: DEBUG
    org.springframework.security: INFO
    org.springframework.boot.test: INFO

cookie:
  secure: false

management:
  endpoints:
    web:
      exposure:
        include: health,info
  info:
    env:
      enabled: true

info:
  app:
    team-name: TestTeam